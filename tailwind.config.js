/** @type {import('tailwindcss').Config} */

const config = {
  content: ["./src/**/*.{md,markdown,liquid,html}"],
  theme: {
    container: {
      center: true
    }
  }
};

module.exports = config;
